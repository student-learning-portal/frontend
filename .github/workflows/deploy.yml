name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest changes
        run: cd ~/frontend && git pull

      - name: Restart containers
        run: cd ~/infra && docker compose up -d --build
